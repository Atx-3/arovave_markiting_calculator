{
  "name": "arovave-pricing-engine",
  "version": "0.1.0",
  "private": true,
  "description": "Dynamic Deterministic Pricing Engine Platform",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev:server": "npm run dev -w packages/server",
    "dev:web": "npm run dev -w packages/web",
    "dev": "npm run dev:server & npm run dev:web",
    "build": "npm run build -w packages/engine && npm run build -w packages/server && npm run build -w packages/web",
    "lint": "eslint . --ext .ts,.tsx",
    "format": "prettier --write \"packages/**/*.{ts,tsx,json,css}\"",
    "test": "npm run test -w packages/engine && npm run test -w packages/server",
    "test:engine": "npm run test -w packages/engine",
    "test:server": "npm run test -w packages/server"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
